spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    database: postgresql
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
  datasource:
    username: ShevchenkoDN
    url: jdbc:postgresql://localhost:5433/storeDb
    driver-class-name: org.postgresql.Driver
    password: She031fit
  sql:
    init:
      platform: postgres

